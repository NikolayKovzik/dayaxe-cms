enum RoutesList {
  DEFAULT = '/',
  USERS = '/users',
  DAYCATION = '/daycation',
  HOTEL_PASSES = '/hotel-passes',
  PROMOTIONS = '/promotions',
  MOMENTS = '/moments',
  NOT_FOUND = '*',
}

export default RoutesList;
